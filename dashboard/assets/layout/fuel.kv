#:kivy 1.11.0
#:import C gauge_constants

<Fuel>:
    RelativeLayout:
        center: (self.parent.center_x + C.LINEAR_ICON_SIZE, self.parent.center_y)
        size: (C.LINEAR_ICON_SIZE, C.LINEAR_ICON_SIZE)
        
        Image:
            source: C.FUEL_ICON_PATH
            fit_mode: 'contain'
            texture_size: self.parent.size