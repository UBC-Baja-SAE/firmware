#:kivy 1.11.0

<Logo>:
    RelativeLayout:
        center: (self.parent.center_x, 300)
        size: (180, 180)
        
        Image:
            source: './assets/images/logo.png'
            fit_mode: 'contain'
            texture_size: self.parent.size