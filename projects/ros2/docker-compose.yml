services:
  ros2:
    build:
      context: .
      dockerfile: Dockerfile.gui
    image: bfrzn/ros2-gui:jazzy
    container_name: mochi_ros2_gui
    stop_grace_period: 300s
    network_mode: "host"
    privileged: true
    restart: unless-stopped
    environment:
      - TZ=America/Vancouver
      - FOXGLOVE_API_KEY=fox_sk_3va2wRx9YT4btXPJxwgCeofNLNHEXJJf
      - FOXGLOVE_DEVICE_ID=dev_0eHNnHWW6p4clvbd
      - QT_QPA_PLATFORM=eglfs
      - QT_QPA_EGLFS_ALWAYS_SET_MODE=1
      - QT_QPA_EGLFS_HIDECURSOR=1
      - XDG_RUNTIME_DIR=/tmp/runtime-root
    volumes:
      - ./mcap_logs:/logs
      - ./gui:/ros_ws/gui
      - ./canopen:/ros_ws/canopen
    devices:
      - "/dev/dri:/dev/dri"
      - "/dev/input:/dev/input"
      - "/dev/gpiomem:/dev/gpiomem"
      - "/dev/gpiochip0:/dev/gpiochip0"
    command: >
      bash -c "
        set -e ; 
        # ... (keep your Foxglove auth/cleanup trap logic)

        source /opt/ros/jazzy/setup.bash ;
        source /canopen_ws/install/setup.bash ; 
        source /ros_ws/install/setup.bash ;

        echo 'Configuring can0...' ;
        ip link set can0 type can bitrate 500000 up 2>/dev/null || echo 'CAN setup failed' ;
      
        # Generate DCF and launch
        cd /ros_ws/canopen && dcfgen bus.yml -d /ros_ws/canopen --no-strict ;
      
        # Launch your processes
        python3 /ros_ws/gui/Backend.py &
        ros2 launch canopen_core canopen.launch.py ... &
        ros2 launch foxglove_bridge foxglove_bridge_launch.xml &
        ros2 bag record -s mcap -o /logs/$(date +%a_%b_%d_%H-%M) -a &
        wait
      "